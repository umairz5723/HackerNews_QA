To setup and run the project:

Run npm install after cloning to get the required modules
Change directory to the backend folder and run "node server.js" in the terminal. This will start the backend server as well as the scraper itself.
Open another terminal and navigate to the frontend folder. Run "npm start" to view the React frontend of this project.